{
  "name": "paybond-vercel-shopping-agent",
  "private": false,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "smoke": "paybond agent sandbox smoke --preset shopping --result-body '{\"status\":\"completed\",\"cost_cents\":4500}' --format json"
  },
  "dependencies": {
    "@paybond/kit": "^0.10.0",
    "ai": "^5.0.0",
    "zod": "^4.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=22"
  }
}
